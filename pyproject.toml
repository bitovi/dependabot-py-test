[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agentclass"
version = "0.0.6"
description = "AgentClass"
readme = "README.md"
requires-python = ">=3.9"

