[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "agentclass"
version = "0.0.6"
description = "AgentClass"
readme = "README.md"
requires-python = ">=3.9"

